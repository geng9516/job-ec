<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="con.chin.mapper.ItemMapper">

    <!--    新产品登录-->
    <insert id="saveItem">

        insert into t_item (sitename,
                            shopname,
                            itemcode,
                            olditemcode,
                            itempath,
                            itemcategorycode,
                            itemname,
                            price,
                            option1,
                            value1,
                            option2,
                            value2,
                            option3,
                            value3,
                            option4,
                            value4,
                            option5,
                            value5,
                            url5,
                            headline,
                            caption,
                            explanation,
                            image,
                            folg,
                            created,
                            enddata)
        values (#{siteName},
                #{shopName},
                #{itemCode},
                #{oldItemCode},
                #{itemPath},
                #{itemCategoryCode},
                #{itemName},
                #{price},
                #{option1},
                #{value1},
                #{option2},
                #{value2},
                #{option3},
                #{value3},
                #{option4},
                #{value4},
                #{option5},
                #{value5},
                #{url5},
                #{headline},
                #{caption},
                #{explanation},
                #{image},
                #{folg},
                #{created},
                #{endData})

    </insert>

    <!--    产品更新-->
    <update id="updateItem">

        update t_item
        set sitename=#{siteName},
            shopname=#{shopName},
            itempath=#{itemPath},
            itemcategorycode=#{itemCategoryCode},
            itemname=#{itemName},
            price=#{price},
            option1=#{option1},
            option1=#{option1},
            value1=#{value1},
            option2=#{option2},
            value2=#{value2},
            option3=#{option3},
            value3=#{value3},
            option4=#{option4},
            value4=#{value4},
            option5=#{option5},
            value5=#{value5},
            headline=#{headline},
            url5=#{url5},
            explanation=#{explanation},
            image=#{image},
            flog=#{flog}
        where oldItemCode = #{oldItemCode}

    </update>

    <!--    检索所以产品-->
    <select id="findAllItem" resultType="con.chin.pojo.Item">

        select *
        from t_item

    </select>

    <update id="setdata">
        update t_item set flog=#{flog},enddata=#{endData} where id = #{id}
    </update>

    <!--    旧产品code检索-->
    <select id="findItem" resultType="con.chin.pojo.Item">

        select *
        from t_item
        where olditemcode = #{oldItemCode}

    </select>

    <!--    旧产品code检索-->
    <select id="findItemByItemCode" resultType="con.chin.pojo.Item">

        select *
        from t_item
        <where>
            <if test="itemCode != null and itemCode != ''">
                or itemcode like concat('%',#{itemCode},'%')
            </if>
            <if test="itemCode != null and itemCode != ''">
                or itempath like '%' #{itemCode} '%'
            </if>
            <if test="itemCode != null and itemCode != ''">
                or itemname like concat('%',#{itemCode},'%')
            </if>
            <if test="itemCode != null and itemCode != ''">
                or shopname like concat('%',#{itemCode},'%')
            </if>
            <if test="itemCode != null and itemCode != ''">
                or olditemcode like concat('%',#{itemCode},'%')
            </if>

        </where>

        order by creaed desc

    </select>

    <!--    获取产品code-->
    <select id="findItemCodeByPath" resultType="string">

        select itemcode
        from t_item
        where itempath = #{path}

    </select>


</mapper>































